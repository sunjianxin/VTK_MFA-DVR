#include <vector>

#define SHADEON

struct sample {
    int j; // row index
    int i; // column index
    double x; // x location
    double y; // x location
    double z; // x location
    double v_linear; // value
    double v_mfa; // value
};
